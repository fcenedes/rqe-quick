# -----------------------------
# Redis Connection Settings
# -----------------------------
# Redis host (e.g., "localhost" or "redis-11127.internal.c48004.eu-west9-mz.gcp.cloud.rlrcp.com")
REDIS_HOST=localhost

# Redis port (e.g., 6379 or 11127)
REDIS_PORT=6379

# Redis database number
REDIS_DB=0

# Redis username (leave empty for no auth, or "default")
REDIS_USERNAME=

# Redis password (leave empty for no auth)
REDIS_PASSWORD=

# -----------------------------
# Performance Settings
# -----------------------------
# NOTE: Batch size is MORE important than parallelism for single Redis instance!
#
# BATCH SIZE CALCULATION (for ecommerce schema ~210 bytes/doc):
#   - MTU: 1,500 bytes (Ethernet standard)
#   - TCP payload: 1,460 bytes (after IP/TCP headers)
#   - Batch 1,000:  210 KB,  144 packets, 97.2% efficiency
#   - Batch 2,000:  420 KB,  288 packets, 97.2% efficiency
#   - Batch 5,000:  1.0 MB,  720 packets, 97.2% efficiency
#   - Batch 10,000: 2.1 MB, 1439 packets, 97.3% efficiency
#
# RECOMMENDATION FOR REDIS CLOUD (remote, 10-50ms latency):
#   - Use larger batches (5K-10K) to hide network latency
#   - Use moderate parallelism (4 workers) for remote connections

# Number of concurrent worker threads
PARALLEL_WORKERS=4

# Size of Redis connection pool (should match PARALLEL_WORKERS)
CONNECTION_POOL_SIZE=4

# Pipeline batch size for seeding
# For Redis Cloud: 10000 recommended (2.1 MB, hides latency)
# For local Redis: 50000 (low latency, maximize throughput)
SEED_BATCH_SIZE=10000

# Cursor batch size for aggregation
# For Redis Cloud: 5000 recommended (balance latency and memory)
# For local Redis: 50000 (maximize throughput)
AGGREGATE_BATCH_SIZE=5000

# -----------------------------
# Data Generation Settings
# -----------------------------
# Random seed for reproducible data generation
# Same seed = same data every time (useful for consistent benchmarks)
# Change seed to generate different random data
RANDOM_SEED=42

# -----------------------------
# Example Configurations
# -----------------------------

# Example 1: Local Redis (no authentication) - DEFAULT
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_USERNAME=
# REDIS_PASSWORD=
# PARALLEL_WORKERS=2
# CONNECTION_POOL_SIZE=2
# SEED_BATCH_SIZE=50000
# AGGREGATE_BATCH_SIZE=50000

# Example 2: Redis Cloud (with authentication) - RECOMMENDED FOR REMOTE
# REDIS_HOST=redis-11127.internal.c48004.eu-west9-mz.gcp.cloud.rlrcp.com
# REDIS_PORT=11127
# REDIS_DB=0
# REDIS_USERNAME=default
# REDIS_PASSWORD=b3tgNv3DU6pD9GlShSyYBZjSAzgPlXIq
# PARALLEL_WORKERS=4
# CONNECTION_POOL_SIZE=4
# SEED_BATCH_SIZE=10000        # 2.1 MB batches hide 10-50ms latency
# AGGREGATE_BATCH_SIZE=5000    # Balance latency and memory

# Example 3: Redis Cluster (multiple shards)
# REDIS_HOST=redis-cluster-node1.example.com
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_USERNAME=default
# REDIS_PASSWORD=cluster-password
# PARALLEL_WORKERS=8
# CONNECTION_POOL_SIZE=8
# SEED_BATCH_SIZE=20000
# AGGREGATE_BATCH_SIZE=20000

